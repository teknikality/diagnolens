// DiagnoLens — Runtime configuration
// Edit API_BASE to point to your backend, or leave empty to auto-detect.
//
// Examples:
//   ''                          → auto-detect from the hostname serving this page
//   'http://localhost:8000'     → always use local dev server
//   'http://192.168.0.107:8000' → always use a specific LAN IP

window.DL_CONFIG = {
  API_BASE: '',
};
